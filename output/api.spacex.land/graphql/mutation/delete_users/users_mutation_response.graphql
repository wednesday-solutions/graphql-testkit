mutation Delete_users($delete_usersWhere: users_bool_exp!) {
  delete_users(where: $delete_usersWhere) {
    affected_rows
    returning {
      id
      name
      rocket
      timestamp
      twitter
    }
  }
}
